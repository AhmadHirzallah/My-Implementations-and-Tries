{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",  // The task name must match the preLaunchTask
            "type": "shell",
            "command": "make",  // Command to run; in your case, it's 'make'
            "args": [],         // Any arguments for 'make'; in this case, none
            "group": {
                "kind": "build",
                "isDefault": true  // This makes it the default build task
            },
            "problemMatcher": ["$gcc"],  // For detecting compiler errors
            "detail": "Generated task for building the project using make."
        }
    ]
}
